# Removed — GitHub Copilot setup is handled inside the configure wizard.
# Run: sarthak configure  (Models & Providers > github-copilot)
raise ImportError("copilot_cli has been removed. Use 'sarthak configure' to set up GitHub Copilot.")
